import { PagePlaceholder } from './PagePlaceholder';

export function CheckoutPage() {
  return (
    <PagePlaceholder
      title="Оформление"
      description="Защищённый экран. Доступен, только если у пользователя есть активное право на покупку этого товара — иначе редирект обратно к товару."
    />
  );
}
